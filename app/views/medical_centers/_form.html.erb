<%= form_with(model: medical_center) do |form| %>
  <% if medical_center.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(medical_center.errors.count, "error") %> prohibited this medical_center from being saved:</h2>

      <ul>
        <% medical_center.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= label_tag "medical_center[institution_id]", "Medical Institution" %>
    <%= select_tag "medical_center[institution_id]", options_for_select(Institution.all.map { |d| [d.name, d.id] }), prompt: "Select an Institution", class: "form-control" %>
  </div>



  <div>
    <%= form.label :location, style: "display: block" %>
    <%= form.text_field :location %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
