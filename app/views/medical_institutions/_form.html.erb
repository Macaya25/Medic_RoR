<%= form_with(model: medical_institution) do |form| %>
  <% if medical_institution.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(medical_institution.errors.count, "error") %> prohibited this medical_institution from being saved:</h2>

      <ul>
        <% medical_institution.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
